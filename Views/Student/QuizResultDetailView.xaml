<UserControl x:Class="e_learning_app.Views.QuizResultDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F8FAFC">

    <UserControl.Resources>
        <!-- Style cho Nút Quay lại -->
        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="0,5">
                            <TextBlock x:Name="Txt" Text="{TemplateBinding Content}" FontSize="14" FontWeight="SemiBold" Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Txt" Property="Foreground" Value="#3B82F6"/>
                                <Setter TargetName="Txt" Property="TextDecorations" Value="Underline"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho Tab Lọc -->
        <Style x:Key="FilterTabStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" CornerRadius="20" Padding="18,8" Background="White" BorderBrush="#E2E8F0" BorderThickness="1">
                            <!-- Thêm hiệu ứng chuyển đổi mượt -->
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal">
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="MouseOver">
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)" To="#93C5FD" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="#64748B"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3B82F6"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#3B82F6"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30,20,30,20">
        <!-- Thêm Margin tổng thể để cách xa viền Window -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Button Content="← Quay lại lịch sử" Style="{StaticResource BackButtonStyle}" HorizontalAlignment="Left" Click="BtnBack_Click"/>

            <TextBlock x:Name="TxtTitle" Text="Chi tiết bài làm" FontSize="26" FontWeight="ExtraBold" Foreground="#0F172A" Margin="0,15,0,4"/>
            <TextBlock x:Name="TxtSubTitle" Text="Nộp lúc 14:30 - 24/10/2023" FontSize="14" Foreground="#64748B" FontWeight="Medium"/>
        </StackPanel>

        <!-- Score Summary Banner -->
        <Border Grid.Row="1" CornerRadius="16" Padding="30,25" Margin="0,0,0,25">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#3B82F6" Offset="0"/>
                    <GradientStop Color="#1D4ED8" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#3B82F6" BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="KẾT QUẢ TỔNG QUAN" FontSize="12" FontWeight="Bold" Foreground="#93C5FD" Margin="0,0,0,6"/>
                    <TextBlock x:Name="TxtResultStatus" Text="Trạng thái: ĐẠT" FontSize="22" FontWeight="Bold" Foreground="White"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtScoreLarge" Text="8.5 / 10" FontSize="36" FontWeight="Black" Foreground="White"/>
                    <TextBlock x:Name="TxtPercentage" Text="Tỷ lệ đúng: 85%" FontSize="14" FontWeight="Medium" Foreground="#DBEAFE" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Filters -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <RadioButton x:Name="FilterAll" Content="Tất cả" IsChecked="True" Style="{StaticResource FilterTabStyle}" Click="Filter_Click" Tag="All"/>
            <RadioButton x:Name="FilterCorrect" Content="Đúng" Style="{StaticResource FilterTabStyle}" Click="Filter_Click" Tag="Correct" Margin="10,0,0,0"/>
            <RadioButton x:Name="RadioButtonIncorrect" Content="Sai" Style="{StaticResource FilterTabStyle}" Click="Filter_Click" Tag="Incorrect" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Questions Detail List -->
        <!-- Đã thêm Padding Right để thanh cuộn không lẹm vào nội dung -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
            <ItemsControl x:Name="ItemsQuestions">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Question Card có DropShadow -->
                        <Border Background="White" CornerRadius="16" Padding="25" Margin="0,0,0,20" BorderBrush="#F1F5F9" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect Color="#CBD5E1" BlurRadius="15" ShadowDepth="2" Opacity="0.2"/>
                            </Border.Effect>
                            <StackPanel>
                                <!-- Question Header -->
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal">
                                        <!-- Bo tròn viên thuốc (CornerRadius lớn) -->
                                        <Border Background="#F8FAFC" CornerRadius="15" Padding="12,6" BorderBrush="#E2E8F0" BorderThickness="1">
                                            <TextBlock Text="{Binding QuestionNumber}" FontWeight="Bold" Foreground="#334155" FontSize="13"/>
                                        </Border>
                                        <Border Margin="10,0,0,0" CornerRadius="15" Padding="12,6" Background="{Binding ResultBg}">
                                            <TextBlock Text="{Binding ResultText}" FontWeight="Bold" Foreground="{Binding ResultFg}" FontSize="13"/>
                                        </Border>
                                    </StackPanel>

                                    <TextBlock Grid.Column="1" Text="{Binding PointsDisplay}" Foreground="#94A3B8" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                                </Grid>

                                <!-- Question Content -->
                                <TextBlock Text="{Binding QuestionContent}" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" TextWrapping="Wrap" Margin="0,0,0,20" LineHeight="26"/>

                                <!-- Options List -->
                                <ItemsControl ItemsSource="{Binding Options}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border x:Name="OptionBorder" Background="{Binding Background}" BorderBrush="{Binding BorderBrush}" 
                                                    BorderThickness="1.5" CornerRadius="12" Padding="16,14" Margin="0,0,0,10">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Text="{Binding Prefix}" FontWeight="Bold" Foreground="{Binding TextBrush}" Margin="0,0,15,0" FontSize="15"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding Content}" Foreground="{Binding TextBrush}" FontSize="15" TextWrapping="Wrap" LineHeight="22" FontWeight="Medium"/>

                                                    <!-- Icon indicator -->
                                                    <TextBlock Grid.Column="2" Text="{Binding Icon}" FontWeight="Black" Foreground="{Binding TextBrush}" FontSize="16" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>