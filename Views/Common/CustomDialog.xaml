<Window x:Class="e_learning_app.CustomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="440"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <Storyboard x:Key="WindowOnLoad">
            <DoubleAnimation Storyboard.TargetName="MainBorder" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="MainScale" Storyboard.TargetProperty="ScaleX" From="0.95" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><BackEase Amplitude="0.2" EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainScale" Storyboard.TargetProperty="ScaleY" From="0.95" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><BackEase Amplitude="0.2" EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Style x:Key="BaseBtn" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="Opacity" Value="0.9"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="border" Property="Opacity" Value="0.8"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#3B82F6"/><Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="SecondaryBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#F1F5F9"/><Setter Property="Foreground" Value="#475569"/>
        </Style>
        <Style x:Key="DangerBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#EF4444"/><Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded"><BeginStoryboard Storyboard="{StaticResource WindowOnLoad}"/></EventTrigger>
    </Window.Triggers>

    <Border x:Name="MainBorder" Margin="30" Opacity="0">
        <Border.RenderTransform><ScaleTransform x:Name="MainScale" CenterX="190" CenterY="100"/></Border.RenderTransform>
        <Border.Effect><DropShadowEffect BlurRadius="40" ShadowDepth="8" Color="#000000" Opacity="0.15" Direction="270"/></Border.Effect>

        <Border Background="White" CornerRadius="20" ClipToBounds="True">
            <StackPanel>
                <!-- Header Area -->
                <Border x:Name="StatusAccent" Background="#3B82F6" Padding="24,20" CornerRadius="20,20,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Width="36" Height="36" CornerRadius="10" x:Name="IconContainer" Background="#FFFFFF20" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtIcon" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI Emoji" Text="ℹ️"/>
                        </Border>

                        <TextBlock x:Name="TxtTitle" Grid.Column="1" Margin="16,0,0,0" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" TextWrapping="Wrap" Text="Thông báo"/>

                        <Button Click="BtnClose_Click" Grid.Column="2" VerticalAlignment="Center" Width="32" Height="32" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent" CornerRadius="16" x:Name="cross">
                                        <TextBlock Text="✕" FontSize="16" Foreground="#FFFFFFCC" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="cross" Property="Background" Value="#FFFFFF20"/><Setter Property="Foreground" Value="White"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>

                <!-- Message & Action Area -->
                <Border Padding="28,24,28,24">
                    <StackPanel>
                        <TextBlock x:Name="TxtMessage" FontSize="15" Foreground="#475569" LineHeight="24" TextWrapping="Wrap" Text="Nội dung thông báo..."/>
                        
                        <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,32,0,0">
                            <!-- Buttons added via code-behind -->
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Border>
</Window>