apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-deployment
  labels:
    app: payment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      imagePullSecrets:
        - name: ghcr-io-registry-key
      containers:
        - name: payment-api
          image: ghcr.io/username/smartedu-payment:latest # Placeholder, updated during CD
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 8081
              name: grpc
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Development"
            - name: MessageBroker__Host
              value: "rabbitmq://messagebroker"
            - name: Redis__ConnectionString
              value: "redis:6379"
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: PAYMENT_DB_CONNECTION
            - name: Jwt__Key
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: JWT_KEY
            - name: Jwt__Issuer
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: JWT_ISSUER
            - name: Jwt__Audience
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: JWT_AUDIENCE
            - name: VnPay__HashSecret
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: VNPAY_HASH_SECRET
            - name: MoMo__SecretKey
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: MOMO_SECRET_KEY
            - name: PayPal__SecretKey
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: PAYPAL_SECRET_KEY
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger:4317"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 8081
      targetPort: 8081
  type: ClusterIP
