apiVersion: apps/v1
kind: Deployment
metadata:
  name: comment-deployment
  labels:
    app: comment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: comment-service
  template:
    metadata:
      labels:
        app: comment-service
    spec:
      imagePullSecrets:
        - name: ghcr-io-registry-key
      containers:
        - name: comment-api
          image: ghcr.io/miyxotkem/se361_microservices/smartedu-comment:latest # Placeholder, updated during CD
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 8081
              name: grpc
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Development"
            - name: Jwt__Key
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: JWT_KEY
            - name: Jwt__Issuer
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: JWT_ISSUER
            - name: Jwt__Audience
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: JWT_AUDIENCE
            - name: FIREBASE_CREDENTIALS_JSON_COMMENT
              valueFrom:
                secretKeyRef:
                  name: smartedu-secrets
                  key: FIREBASE_CREDENTIALS_JSON_COMMENT
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger:4317"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: comment-service
spec:
  selector:
    app: comment-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 8081
      targetPort: 8081
  type: ClusterIP
